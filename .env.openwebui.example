# Open WebUI Test Configuration
# Copy this to .env to use with compose-openwebui.yaml or compose-openwebui-https.yaml

# Use existing LiteLLM configuration from main .env file
# Or set these values for testing:

# REQUIRED: Master key for LiteLLM API access
LITELLM_MASTER_KEY=sk-your-test-key-here

# Database configuration
POSTGRES_PASSWORD=your-test-db-password
DATABASE_URL=postgresql://llmproxy:your-test-db-password@db:5432/litellm

# Claude OAuth token (from main setup)
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token-here

# Open WebUI specific ports
OPEN_WEBUI_PORT=8090
OPEN_WEBUI_HTTPS_PORT=8443

# Optional: Set a secret key for Open WebUI sessions
WEBUI_SECRET_KEY=your-random-secret-key-here